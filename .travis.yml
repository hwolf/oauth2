
sudo: false

language: java
jdk:
  - oraclejdk8

before_install:
  - "export DISPLAY=:99.0"
  - "sh -e /etc/init.d/xvfb start"

script:
  - ./gradlew ciBuild

after_success:
  - find . -name jacoco\*.xml
  - bash <(curl -s https://codecov.io/bash) -v
